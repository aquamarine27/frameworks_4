services:
  frameworks_4:
    build: .
    ports:
      - "3000:3000"
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:3000/healthz/ready"]
      interval: 10s
      timeout: 3s
      retries: 3